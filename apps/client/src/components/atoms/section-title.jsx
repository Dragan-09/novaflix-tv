import React from 'react'

function Title({title}) {
  return (
    <div className='py-5 text-primary uppercase text-2xl sm:text-3xl font-semibold text-center dark:text-white'>{title}</div>
  )
}

export default Title